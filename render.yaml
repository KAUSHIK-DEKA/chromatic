services:
  - type: web
    name: chromatic-color-extractor
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT --app-dir api
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
